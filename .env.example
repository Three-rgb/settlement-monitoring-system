# 数据库连接配置（本地开发模板）
# 使用方法：
#   1. 复制本文件为 .env：  cp .env.example .env
#   2. 填写实际值
#   3. .env 已加入 .gitignore，请勿提交

DB_HOST=localhost
DB_PORT=5432
DB_NAME=construction_db
DB_USER=postgres

# 必填项：未设置会导致 config.py 导入失败
DB_PASSWORD=replace_me_with_real_password