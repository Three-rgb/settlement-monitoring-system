# Airflow docker-compose 配置模板
# 复制为 .env 并修改对应值

# 业务仓库在宿主机上的绝对路径（Windows 示例）
# DockerOperator 启动任务容器时会把它 bind-mount 到容器内
HOST_REPO_ROOT=C:/settlement_monitoring_system

# 业务数据库密码（与项目根 .env 保持一致）
DB_PASSWORD=123456